/*
Весь этот интерфейс построен на трех сценариях взаимодействия: «Случайная», «Старт» и «Стоп».

Вот как я это вижу и как это должно работать в твоем проекте:

1. Кнопка «Случайная» (Ручной режим)
Эта кнопка для тех случаев, когда пользователь хочет получить одну конкретную цитату, а не ждать, пока они сменятся сами.

Логика:

При нажатии она берет одну случайную цитату из твоего массива.

Мгновенно меняет текст на странице.

Важное правило UX: Если в этот момент крутится автопоказ (таймер), нажатие на «Случайная» должно останавливать таймер. Пользователь сказал: «Я хочу эту конкретную цитату сейчас, убери автопоказ».

2. Кнопка «▶ Старт» (Автоматический режим)
Запускает бесконечный цикл смены цитат. Это как слайд-шоу.

Логика:

При нажатии включается JavaScript-таймер (setInterval). Например, он будет срабатывать каждые 3 или 5 секунд.

Каждый раз, когда таймер "тикает", код берет новую случайную цитату и обновляет текст на экране.

Важное правило: Если таймер уже запущен, при повторном нажатии на «Старт» ничего страшного не должно произойти. Либо кнопка должна игнорироваться, либо таймер должен сброситься и начать отсчет заново.

3. Кнопка «■ Стоп» (Остановка режима)
Останавливает автоматическую смену и оставляет на экране ту цитату, которая была показана последней.

Логика:

При нажатии выполняется команда clearInterval(). Это убивает таймер, который был запущен кнопкой «Старт».

Текст на экране застывает и больше не меняется, пока пользователь снова не нажмет «Старт» или «Случайная».
*/

import { arrayOfQuotes } from "./arrayOfQuotes";

const btnStart = document.querySelector(".btn-start");

function start(){}